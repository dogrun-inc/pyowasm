# Pyowasm プロジェクト規約

本書は、**Pyowasm** プロジェクトのアーキテクチャ、コーディング規約、およびワークフローを定義します。すべての開発者および AI エージェントは、これらのガイドラインに従う必要があります。

## 1. アーキテクチャ

Pyowasm は、拡張性の高いワークフロー実行エンジンを備えたブラウザベースのバイオ情報学プラットフォームです。

- **フロントエンド:** [stlite](https://github.com/whitphx/stlite)
- **コアロジック:** `src/pyowasm/` に集約

### ディレクトリ構成
- `src/pyowasm/app.py`: UI エントリポイント
- `src/pyowasm/engine/`: ワークフロー実行エンジン（runner, context）
- `src/pyowasm/tasks/`: 解析タスクの定義
    - `local/`: Pyodide 内処理（BioPython 等）
    - `remote/`: 外部連携（NCBI API, Docker 等）
    - `wasm/`: JS ブリッジ経由の Wasm ツール
- `src/pyowasm/ui/`: UI コンポーネント（parts, sidebar）
- `src/pyowasm/models/`: 共通データ構造（schema）
- `assets/`: 静的ファイル（ロゴ、サンプルデータ）
- `src/pyowasm/bridge/`: JS/Wasm ブリッジ実装

## 2. コーディング規約

### 言語
- **コミュニケーション:** AI とのやり取りおよびプロジェクト内のドキュメントは日本語を使用します。
- **Docstring:** すべてのクラスおよびメソッドに、**日本語**で docstring を記述してください。

### 型ヒント
- **型ヒント:** すべてのメソッドおよびその引数に必ず型ヒントを記述してください。

### テスト
- **パブリックメソッド:** すべてのパブリックメソッドに対して、ユニットテストを必須とします。
- **フレームワーク:** `pytest` を使用します。

## 3. ワークフロー

## 3. ワークフロー

### ブランチ管理
- すべての機能開発、バグ修正、およびIssue単位の実装は、必ず `develop` ブランチから分岐した専用のブランチで行ってください。
- ブランチ名は `feature/<機能名>` または `fix/<バグ名>` のプレフィックスを使用してください。
- 実装が完了し、ユニットテストおよび検証が完了した後、`develop` ブランチへマージしてください。
- 直接 `develop` や `main` ブランチへコミットすることを固く禁じます。

### コミット
- コミットは小さく、焦点を絞って行ってください。
- 1コミットあたりの変更量は、最大 50 行程度を目安にしてください。
- コミットメッセージには、変更内容（feat, fix, docs, chore 等）を含めてください。

### 検証
- 実装完了時や重要な区切りで、必ず `pytest` を実行してください。
- コミット前に、コードが定められたディレクトリ構成と規約に従っているか確認してください。
